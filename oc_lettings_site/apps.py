from django.apps import AppConfig


class OCLettingsSiteConfig(AppConfig):
    """
    Django application configuration for the 'oc_lettings_site' app.

    This class configures the 'oc_lettings_site' application. It helps Django
    to identify and set up
    the application correctly. The name attribute is used to specify the
    application's name in the project.
    """
    name = 'oc_lettings_site'
