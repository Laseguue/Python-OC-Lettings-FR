from django.apps import AppConfig


class LettingConfig(AppConfig):
    """
    Django application configuration for the 'lettings' app.

    This class configures the 'lettings' application. It is required for
    Django to recognize the app
    and its configurations. The name attribute specifies the name of the
    application.
    """
    name = 'lettings'
